import os
import sys  # unused import


def bad_function(a, b, c):  # no spaces after commas
    x = [1, 2, 3, 4, 5]  # no spaces in list
    if x:
        print("hello world")  # extra spaces
    return a + b + c  # no spaces around operators


def another_function():
    pass
